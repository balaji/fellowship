buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'org.gradle.api.plugins:gradle-android-plugin:1.2.1'
    }
}

subprojects {
    repositories {
        mavenCentral()
    }

    version = "1.0-SNAPSHOT"
}
